<%= form_with(model: playlistsong, local: true) do |form| %>
  <% if playlistsong.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(playlistsong.errors.count, "error") %> prohibited this playlistsong from being saved:</h2>

      <ul>
        <% playlistsong.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :song_id %>
    <%= form.select :song_id, Song.all.collect {|u| [u.name, u.id]}, :prompt => 'Select' %>
  </div>

  <div class="field">
    <%= form.label :playlist_id %>
    <%= form.select :playlist_id, Playlist.all.collect {|u| [u.name, u.id]}, :prompt => 'Select' %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
